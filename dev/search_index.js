var documenterSearchIndex = {"docs":
[{"category":"section","location":"lib/#Library-documentation","page":"Library","text":"","title":"Library documentation"},{"category":"type","location":"lib/#OptimKit.AndersonMixing","page":"Library","text":"struct AndersonMixing{T <: Real} <: FixedPointAlgorithm\nAndersonMixing(m::Int;\n                  damping::Real = 1,\n                  maxiter::Int=MAXITER[], # 1_000_000\n                  gradtol::Real=GRADTOL[], # 1e-8\n                  verbosity::Int=VERBOSITY[]) # 1\n\nAnderson mixing, also known as Anderson acceleration, for fixed point problems.\n\nwarning: Warning\nExperimental implementation – subject to change.\n\nParameters\n\nm::Int: The number of previous iterates to use for Anderson extrapolation.\ndamping::Real: The damping parameter for Anderson extrapolation; a value of 1 corresponds to no damping, while a value between 0 and 1 corresponds to under-relaxation.\nmaxiter::Int: The maximum number of iterations.\ngradtol::T: The tolerance for the norm of the residual.\nverbosity::Int: The verbosity level of the optimization algorithm.\n\nThe verbosity level use the following scheme:\n\n0: no output\n1: only warnings upon non-convergence\n2: convergence information at the end of the algorithm\n3: progress information after each iteration\n\n\n\n\n\n","title":"OptimKit.AndersonMixing"},{"category":"type","location":"lib/#OptimKit.ConjugateGradient","page":"Library","text":"struct ConjugateGradient{F<:CGFlavor,T<:Real,L<:AbstractLineSearch} <: OptimizationAlgorithm\nConjugateGradient(;\n                  flavor::CGFlavor=HagerZhang(),\n                  restart::Int=typemax(Int);\n                  maxiter::Int=MAXITER[], # 1_000_000\n                  gradtol::Real=GRADTOL[], # 1e-8\n                  verbosity::Int=VERBOSITY[], # 1\n                  ls_maxiter::Int=LS_MAXITER[], # 10\n                  ls_maxfg::Int=LS_MAXFG[], # 20\n                  ls_verbosity::Int=LS_VERBOSITY[], # 1\n                  linesearch = HagerZhangLineSearch(maxiter=ls_maxiter, maxfg=ls_maxfg, verbosity=ls_verbosity))\n\nConjugateGradient optimization algorithm.\n\nParameters\n\nflavor: The flavor of the conjugate gradient algorithm (for selecting the β parameter; see below)\nrestart::Int: The number of iterations after which to reset the search direction.\nmaxiter::Int: The maximum number of iterations.\ngradtol::T: The tolerance for the norm of the gradient.\nverbosity::Int: The verbosity level of the optimization algorithm.\nls_maxiter::Int: The maximum number of iterations for the line search.\nls_maxfg::Int: The maximum number of function evaluations for the line search.\nls_verbosity::Int: The verbosity level of the line search algorithm.\nlinesearch: The line search algorithm to use; if a custom value is provided, it overrides ls_maxiter, ls_maxfg, and ls_verbosity.\n\nBoth verbosity levels use the following scheme:\n\n0: no output\n1: only warnings upon non-convergence\n2: convergence information at the end of the algorithm\n3: progress information after each iteration\n4: more detailed information (only for the linesearch)\n\nThe flavor parameter can take the values\n\nHagerZhang(; η::Real=4 // 10, θ::Real=1 // 1): Hager-Zhang formula for β\nHestenesStiefel(; pos = true): Hestenes-Stiefel formula for β\nFletcherReeves(): Fletcher-Reeves formula for β\nPolakRibiere(; pos = true): Polak-Ribiere formula for β\nDaiYuan(): Dai-Yuan formula for β\n\n\n\n\n\n","title":"OptimKit.ConjugateGradient"},{"category":"type","location":"lib/#OptimKit.GradientDescent","page":"Library","text":"GradientDescent(; \n                maxiter::Int=MAXITER[], # 1_000_000\n                gradtol::Real=GRADTOL[], # 1e-8\n                verbosity::Int=VERBOSITY[], # 1\n                ls_maxiter::Int=LS_MAXITER[], # 10\n                ls_maxfg::Int=LS_MAXFG[], # 20\n                ls_verbosity::Int=LS_VERBOSITY[], # 1\n                linesearch = HagerZhangLineSearch(maxiter=ls_maxiter, maxfg=ls_maxfg, verbosity=ls_verbosity))\n\nGradient Descent optimization algorithm.\n\nParameters\n\nmaxiter::Int: The maximum number of iterations.\ngradtol::T: The tolerance for the norm of the gradient.\nverbosity::Int: The verbosity level of the optimization algorithm.\nls_maxiter::Int: The maximum number of iterations for the line search.\nls_maxfg::Int: The maximum number of function evaluations for the line search.\nls_verbosity::Int: The verbosity level of the line search algorithm.\nlinesearch: The line search algorithm to use; if a custom value is provided, it overrides ls_maxiter, ls_maxfg, and ls_verbosity.\n\nBoth verbosity and ls_verbosity use the following scheme:\n\n0: no output\n1: only warnings upon non-convergence\n2: convergence information at the end of the algorithm\n3: progress information after each iteration\n4: more detailed information (only for the linesearch)\n\n\n\n\n\n","title":"OptimKit.GradientDescent"},{"category":"type","location":"lib/#OptimKit.HagerZhangLineSearch","page":"Library","text":"(ls::HagerZhangLineSearch)(fg, x₀, η₀, fg₀ = fg(x₀);\n                retract = _retract, inner = _inner,\n                initialguess = one(fg₀[1]), acceptfirst = false,\n                maxiter = ls.maxiter, maxfg = lsmaxfg, verbosity = ls.verbosity)\n\nPerform a Hager-Zhang line search to find a step length that satisfies the (approximate) Wolfe conditions.\n\nArguments:\n\nls::HagerZhangLineSearch: The HagerZhangLineSearch object.\nfg: Function that computes the objective function and its gradient.\nx₀: Starting point.\nη₀: Search direction.\nfg₀: Objective function and gradient evaluated at x₀. Defaults to fg(x₀), but can be supplied if this information has already been calculated.\n\nKeyword Arguments:\n\nretract: Function that performs the retraction step, i.e. the generalisation of x₀ + α * η₀. Defaults to _retract.\ninner: Function that computes the inner product between search direction and gradient. Defaults to _inner.\ninitialguess::Real: Initial guess for the step length. Defaults to one(fg₀[1]).\nacceptfirst::Bool: Parameter that controls whether the initial guess can be accepted if it satisfies the strong Wolfe conditions. Defaults to false, thus requiring  at least one line search iteration and one extra function evaluation.\nmaxiter::Int: Hard limit on the number of iterations. Default is 50.\nmaxfg::Int: Soft limit on the number of function evaluations. Default is 100.\nverbosity::Int: The verbosity level (see below). Default is 0.\n\nVerbosity Levels\n\n0: No output.\n1: Single output about convergence when the linesearch has terminated.\n2: Output after the start and every individual iteration step of the Hager-Zhang linesearch.\n3: Additional output about the initial bracketing and further bracket update and bisection steps.\n4: Output after every function evaluation in the bracketing, updating, and bisection steps.\n\nReturns:\n\nx: The point retract(x₀, η₀, α) where the (approximate) Wolfe conditions are satisfied.\nf: Function value at x.\ng: Gradient at x.\nξ: Tangent at x to the line search path.\nα: Step length that satisfies the (approximate) Wolfe conditions.\nnumfg: Number of function evaluations performed.\n\n\n\n\n\n","title":"OptimKit.HagerZhangLineSearch"},{"category":"method","location":"lib/#OptimKit.HagerZhangLineSearch-Tuple{}","page":"Library","text":"struct HagerZhangLineSearch{T<:Real} <: AbstractLineSearch\nHagerZhangLineSearch(; c₁::Real = 1//10, c₂::Real = 9//10, ϵ::Real = 1//10^6,\n                    θ::Real = 1//2, γ::Real = 2//3, ρ::Real = 5//1)\n\nConstructs a Hager-Zhang line search object with the specified parameters.\n\nArguments:\n\nc₁::Real: Parameter for the (approximate) first Wolfe condition (Armijo rule), controlling sufficient decay of function value: c₁ < 1/2 < c₂. Default is 1//10.\nc₂::Real: Parameter for the second Wolfe condition (curvature contition), controlling sufficient decay of slope: c₁ < 1/2 < c₂. Default is 9//10.\nϵ::Real: Parameter for expected accuracy of objective function, controlling maximal allowed increase in function value. Default is 1//10^6.\nθ::Real: Parameter regulating the bisection step. Default is 1//2 (should probably not be changed).\nγ::Real: Parameter triggering the bisection step, namely if bracket reduction rate is slower than γ. Default is 2//3.\nρ::Real: Parameter controlling the initial bracket expansion rate. Default is 5//1.\nmaxiter::Int: Hard limit on the number of iterations. Default is OptimKit.LS_MAXITER[] (set to 50).\nmaxfg::Int: Soft limit on the number of function evaluations. Default is OptimKit.LS_MAXFG[] (set to 100).\nverbosity::Int: The verbosity level (see below). Default is OptimKit.LS_VERBOSITY[] (set to 0).\n\nReturns:\n\nThis method returns a HagerZhangLineSearch object ls, that can then be can be applied as ls(fg, x₀, η₀; kwargs...) to perform a line search for function fg that computes the objective function and its gradient at a given point, starting from x₀ in direction η₀.\n\n\n\n\n\n","title":"OptimKit.HagerZhangLineSearch"},{"category":"type","location":"lib/#OptimKit.LBFGS","page":"Library","text":"LBFGS(m::Int = 8; \n      acceptfirst::Bool = true,\n      maxiter::Int=MAXITER[], # 1_000_000\n      gradtol::Real=GRADTOL[], # 1e-8\n      verbosity::Int=VERBOSITY[], # 1\n      ls_maxiter::Int=LS_MAXITER[], # 10\n      ls_maxfg::Int=LS_MAXFG[], # 20\n      ls_verbosity::Int=LS_VERBOSITY[], # 1\n      linesearch = HagerZhangLineSearch(maxiter=ls_maxiter, maxfg=ls_maxfg, verbosity=ls_verbosity))\n\nLBFGS optimization algorithm.\n\nParameters\n\nm::Int: The number of previous iterations to store for the limited memory BFGS approximation.\nmaxiter::Int: The maximum number of iterations.\ngradtol::T: The tolerance for the norm of the gradient.\nverbosity::Int: The verbosity level of the optimization algorithm.\nacceptfirst::Bool: Whether to accept the first step of the line search.\nls_maxiter::Int: The maximum number of iterations for the line search.\nls_maxfg::Int: The maximum number of function evaluations for the line search.\nls_verbosity::Int: The verbosity level of the line search algorithm.\nlinesearch: The line search algorithm to use; if a custom value is provided, it overrides ls_maxiter, ls_maxfg, and ls_verbosity.\n\nBoth verbosity and ls_verbosity use the following scheme:\n\n0: no output\n1: only warnings upon non-convergence\n2: convergence information at the end of the algorithm\n3: progress information after each iteration\n4: more detailed information (only for the linesearch)\n\n\n\n\n\n","title":"OptimKit.LBFGS"},{"category":"type","location":"lib/#OptimKit.SimpleIteration","page":"Library","text":"struct SimpleIteration{T <: Real} <: FixedPointAlgorithm\nSimpleIteration(;\n                maxiter::Int=MAXITER[], # 1_000_000\n                gradtol::Real=GRADTOL[], # 1e-8\n                verbosity::Int=VERBOSITY[]) # 1\n\nSimple iteration for fixed point problems.\n\nParameters\n\nmaxiter::Int: The maximum number of iterations.\ngradtol::Real: The tolerance for the norm of the residual.\nverbosity::Int: The verbosity level of the optimization algorithm.\n\nThe verbosity level use the following scheme:\n\n0: no output\n1: only warnings upon non-convergence\n2: convergence information at the end of the algorithm\n3: progress information after each iteration\n\n\n\n\n\n","title":"OptimKit.SimpleIteration"},{"category":"function","location":"lib/#OptimKit.fixedpoint","page":"Library","text":"fixedpoint(fp, x, alg;\n              finalize! = _finalize!,\n              shouldstop = DefaultShouldStop(alg.maxiter),\n              hasconverged = DefaultHasConverged(alg.gradtol),\n              retract = _retract, invretract = _invretract,\n              inner = _inner, (transport!) = _transport!,\n              (scale!) = _scale!, (add!) = _add!,\n              isometrictransport = (transport! == _transport! && inner == _inner))\n-> x, g, numfp, history\n\nFind a fixed point of the function fp starting from an initial point x₀ and using the fixed point algorithm alg, which is an instance of SimpleIteration or AndersonMixing.\n\nReturns the final point x, the residual g, the total number of calls to fp, and the history of the norm of the residual across the different iterations.\n\nThe algorithm is run until either hasconverged(x, false, g, normg) returns true or  shouldstop(x, false, g, numfp, numiter, time) returns true. The latter case happening before the former is considered to be a failure to converge, and a warning is issued.\n\nThe keyword arguments are:\n\nfinalize!::Function: A function that takes the final point x, a dummy variable f = false,   the residual g, and the iteration number, and returns a possibly modified values for   x, f and g. By default, the identity is used.   It is the user's responsibility to ensure that the modified values do not lead to   inconsistencies within the optimization algorithm.\nhasconverged::Function: A function that takes the current point x, a dummy variable f = false,   the residual r, and the norm of the residual, and returns a boolean indicating whether   the fixed point iteration has converged. By default, the norm of the residual is compared to the   tolerance gradtol as encoded in the algorithm instance.\nshouldstop::Function: A function that takes the current point x, a dummy variable f = false,   the residuals g, the number of calls to fg, the iteration number, and the time spent   so far, and returns a boolean indicating whether the optimization should stop. By default,   the number of iterations is compared to the maximum number of iterations as encoded in the   algorithm instance.\n\nCheck the README of this package for further details on creating an algorithm instance, as well as for the meaning of the remaining keyword arguments and their default values.\n\nwarning: Warning\nThe default values of hasconverged and shouldstop are provided to ensure continuity with the previous versions of this package. However, this behaviour might change in the future.\n\nAlso see SimpleIteration and AndersonMixing.\n\n\n\n\n\n","title":"OptimKit.fixedpoint"},{"category":"function","location":"lib/#OptimKit.optimize","page":"Library","text":"optimize(fg, x, alg;\n              precondition = _precondition,\n              (finalize!) = _finalize!,\n              hasconverged = DefaultHasConverged(alg.gradtol),\n              shouldstop = DefaultShouldStop(alg.maxiter),\n              retract = _retract,\n              inner=_inner,\n              transport! = _transport!,\n              scale! =_scale!,\n              add! = _add!,\n              isometrictransport = (transport! == _transport! && inner == _inner))\n-> x, f, g, numfg, history\n\nOptimize (minimize) the objective function returned as the first value of fg, where the second value contains the gradient, starting from a point x and using the algorithm algorithm, which is an instance of GradientDescent, ConjugateGradient or LBFGS.\n\nReturns the final point x, the coresponding function value f and gradient g, the total number of calls to fg, and the history of the gradient norm across the different iterations.\n\nThe algorithm is run until either hasconverged(x, f, g, norm(g)) returns true or shouldstop(x, f, g, numfg, numiter, time) returns true. The latter case happening before the former is considered to be a failure to converge, and a warning is issued.\n\nThe keyword arguments are:\n\nprecondition::Function: A function that takes the current point x and the gradient g   and returns a preconditioned gradient. By default, the identity is used.\nfinalize!::Function: A function that takes the final point x, the function value f,   the gradient g, and the iteration number, and returns a possibly modified values for   x, f and g. By default, the identity is used.   It is the user's responsibility to ensure that the modified values do not lead to   inconsistencies within the optimization algorithm.\nhasconverged::Function: A function that takes the current point x, the function value f,   the gradient g, and the norm of the gradient, and returns a boolean indicating whether   the optimization has converged. By default, the norm of the gradient is compared to the   tolerance gradtol as encoded in the algorithm instance.\nshouldstop::Function: A function that takes the current point x, the function value f,   the gradient g, the number of calls to fg, the iteration number, and the time spent   so far, and returns a boolean indicating whether the optimization should stop. By default,   the number of iterations is compared to the maximum number of iterations as encoded in the   algorithm instance.\n\nCheck the README of this package for further details on creating an algorithm instance, as well as for the meaning of the remaining keyword arguments and their default values.\n\nwarning: Warning\nThe default values of hasconverged and shouldstop are provided to ensure continuity with the previous versions of this package. However, this behaviour might change in the future.\n\nAlso see GradientDescent, ConjugateGradient, LBFGS.\n\n\n\n\n\n","title":"OptimKit.optimize"},{"category":"function","location":"lib/#OptimKit.optimtest","page":"Library","text":"optimtest(fg, x, [d]; alpha = -0.1:0.001:0.1, retract = _retract, inner = _inner)\n-> αs, fs, dfs1, dfs2\n\nTest the compatibility between the computation of the gradient, the retraction and the inner product by computing the derivative of the objective function along a curve corresponding to a retraction starting at the point x in the direction d in two different ways. In particular, at point αs which are in the middle of the points in the original range or list alpha, both the function value fs as well as the two different values for the derivative dfs1 and dfs2 are returned, where the derivatives are computed by\n\nnumerical differentation, i.e. dfs1 contains the values (fg(retract(x, d, alpha[i+1])[1])[1] - fg(retract(x, d, alpha[i])[1])[1])/(alpha[i+1]-alpha[i]) as an estimate for the derivative at the point (alpha[i]+alpha[i+1])/2\nusing the gradient, i.e. dfs2 contains the values inner(xα, dα, gα) where xα, dα = retract(x, d, α) for values α = (alpha[i]+alpha[i+1])/2 and gα = fg(xα)[2].\n\nIt is up to the user to check that the values in dfs1 and dfs2 match up to expected precision, by inspecting the numerical values or plotting them. If these values don't match, the linesearch in optimize cannot be expected to work.\n\n\n\n\n\n","title":"OptimKit.optimtest"},{"category":"section","location":"#OptimKit.jl","page":"Home","text":"A blissfully ignorant Julia package for optimization and fixed-point iteration.\n\nOptimKit is designed to make as few assumptions as possible about your state type and geometry. It supports both classic (Euclidean) vector problems, as well as problems where the configuration space has the structure of a Riemannian manifold, through user-provided retract, transport!, inner, and related callbacks.","title":"OptimKit.jl"},{"category":"section","location":"#Optimization-API","page":"Home","text":"The main functionality of this package is to mimize a cost function f using gradient-based optimization techniques.\n\nx, fx, gx, numfg, history = optimize(fg, x0, algorithm; kwargs...)\n\nHere, the optimization problem (objective function) is specified as a function fval, gval = fg(x) that returns both the function value and its gradient at a given point x. The function value fval is assumed to be a real number of some type T<:Real. Both x and the gradient gval can be of any type, including tuples and named tuples. The keyword arguments to optimize specify how to deal with such general points and gradients, as discussed in the first subsection below. The third positional argument specifies which algorithm to use, and its possible values are discussed in the \"Optimization algorithms\" subsection below.\n\nThe return value consists of\n\nx: final iterate\nfx, gx: cost function value and gradient at the final iterate, i.e. fx, gx = fg(x).\nnumfg: number of calls to fg\nhistory: function values and gradient norms over the different iterations, returned as matrix of size (numiter, 2)","title":"Optimization API"},{"category":"section","location":"#Keyword-arguments-related-to-the-geometry","page":"Home","text":"The first list of keyword arguments is given by retract, inner, scale!, add! and transport!. Their values should be functions that specify the manifold structure and of the configuration space and its tangent space, with the following signature and behavior:\n\nx, ξ = retract(x₀, η, α): take a step in direction η (same type as gradients)    starting from point x₀ and with step length α, returns the new    x(α) = Rₓ₀(α * η) and the local tangent to this path at that position, i.e.    ξ = D Rₓ₀(α * η)η (informally, ξ = dx(α)  dα).\ns = inner(x, ξ1, ξ2): compute the inner product between two gradients or similar    objects at position x. The x dependence is useful for optimization on manifolds,    where this function represents the Riemannian metric; in particular it should be    symmetric inner(x, ξ1, ξ2) == inner(x, ξ2, ξ1) and real-valued.\nη = scale!(η, β): compute the equivalent of η*β, possibly in place, but we always    use the return value. This is mostly used as scale!(g, -1) to compute the negative    gradient as part of the step direction.\nη = add!(η, ξ, β): compute the equivalent of η + ξ*β, possibly overwriting η in    place, but we always use the return value\nξ = transport!(ξ, x, η, α, x′): transport tangent vector ξ along the retraction of    x in the direction η (same type as a gradient) with step length α, can be in    place but the return value is used. Transport also receives x′ = retract(x, η, α)[1]    as final argument, which has been computed before and can contain useful data that does    not need to be recomputed\n\nThe default values for these keyword arguments assume the case of a real Euclidean vector space, and are specified using methods from VectorInterface.jl.\n\nretract(x, η, α) = (VectorInterface.add(x, d, α), d)\ninner(x, ξ1, ξ2) = ξ1 === ξ2 ? LinearAlgebra.norm(ξ1)^2 : real(VectorInterface.inner(ξ1, ξ2))\ntransport!(ξ, x, η, α, xnew) = ξ\nadd!(η, ξ, β) = VectorInterface.add!!(η, ξ, β)\nscale!(η, β) = VectorInterface.scale!!(η, β)\n\nThese default values enable the use of real- or complex-valued arrays, as well as nested structures involving tuples and arrays, as supported by VectorInterface.jl, under the assumption of a Euclidean vector space structure.\n\nTo change the geometrical structure or enable the use of other data types, custom implementations for these functions need to be provided through the keyword arguments, and correctness thereof should be ensured by the user. In particular, note that the gradient g of the objective function should satisfy d f(x(α))  d α  = inner(x(α) ξ(α) g(x(α))). There is a utility function optimtest to facilitate testing this compatibility relation for your given choice of fg, retract and inner.\n\nThere is one more keyword argument related to the geometry, namely isometrictransport::Bool, which is used to indicate whether the transport of vectors preserves their inner product, i.e. whether\n\ninner(x, ξ1, ξ2) == inner(retract(x, η, α), transport!(ξ1, x, η, α), transport!(ξ2, x, η, α))\n\nThe default value is false, unless the default transport and inner product are used. When the inner product is preserved, this can reduce the number of calls to inner, and can also be exploited in some of the optimization algorithms (see below).","title":"Keyword arguments related to the geometry"},{"category":"section","location":"#Additional-keyword-arguments","page":"Home","text":"The optimize function accepts more keyword arguments that can be used to control the optimization process, namely precondition, finalize!, hasconverged and shouldstop. These too should have a function as value with the following signature and behavior:\n\nPη = precondition(x, η): apply a preconditioner to the current gradient or tangent    vector η at the position x; the resulting Pη is assumed to not share memory with    η even if a trivial preconditioner is applied.\nx, f, g = finalize!(x, f, g, numiter): after every step (i.e. upon completion of the    linesearch), allows to modify the position and corresponding function value or    gradient, or to do other tasks such as printing out statistics. Note that this step    happens before computing new directions in Conjugate Gradient and LBFGS, so if f and    g are modified, this is at the user's own risk (e.g. Wolfe conditions might no longer    be satisfied, ...).\nbool = hasconverged(x, f, g, normg): a function that decides whether the optimization    has converged and can thus be stopped (return value true) based on the point x, the    function value f, the gradient g and the gradient norm normg.\nbool = shouldstop(x, f, g, numfg, numiter, t_elapsed): a function that decides whether   the optimization should halt without converging (return value true) based on the point   x, the function value f, the gradient g, the total number numfg of fg calls,   the total number of iterations numiter in the optimization routine, and the total time   t_elapsed since entering the optimize call.\n\nDefault values are given by precondition(x, η) = deepcopy(η), finalize!(x, f, g, numiter) = x, f, g, hasconverged(x, f, g, normg) = normg < algorithm.gradtol and shouldstop(x, f, g, numfg, numiter, t_elapsed) = numiter > algorithm.maxiter, where the algorithm fields are explained in the next subsection.","title":"Additional keyword arguments"},{"category":"section","location":"#Optimization-algorithms","page":"Home","text":"The third positional argument to optimize can take on of the following values\n\nGradientDescent(; kwargs...)\nConjugateGradient(; flavor = ..., kwargs...)\nLBFGS(m::Int; kwargs...)\n\nAll three algorithms share the following keyword arguments:\n\nmaxiter: maximum number of iterations (default effectively unbounded)\ngradtol: stop when norm(g) drops below this threshold (default 1e-8)\nverbosity: controls logging verbosity\nlinesearch: line search algorithm (currently only HagerZhangLineSearch, see below)\n\nLBFGS also takes:\n\npositional argument m::Int: history size for inverse-Hessian approximation\nacceptfirst::Bool = true: whether the first line-search trial step may be accepted\n\nConjugateGradient also takes:\n\nflavor = HagerZhang(; η = 0.4, θ = 1.0) (default)\nflavor = HestenesStiefel()\nflavor = PolakRibiere()\nflavor = DaiYuan()\n\nNote that maxiter and gradtol will be ignored if the values to the keyword arguments hasconverged and shouldstop of the optimize call are changed away from their default values.\n\nIn the case of isometrictransport == true, the convergence of conjugate gradient and LBFGS is more robust (and can be theoretically proven). Note that isometric transport might not be the same as retraction transport. In particular, for x′, ξ = retract(x, η, α), it is possible that ξ = transport(η x η α x). However, when isometric transport is provided, we complement it with an isometric rotation such that ξ = D Rₓ₀(α * η)η and transport(η x η α) are parallel in the case of LBFGS. This is the so-called locking condition of Huang, Gallivan and Absil, and the approach is described in section 4.1.","title":"Optimization algorithms"},{"category":"section","location":"#Line-search","page":"Home","text":"The linesearch keyword argument to the optimization algorithms takes the value.\n\nHagerZhangLineSearch(; δ::Real = .1,\n                       σ::Real = .9,\n                       ϵ::Real = 1e-6,\n                       θ::Real = 1/2,\n                       γ::Real = 2/3,\n                       ρ::Real = 5.,\n                       verbosity::Int = 0)\n\nThe line search has its own verbosity. By default it tracks the optimization algorithm verbosity as verbosity - 2.","title":"Line search"},{"category":"section","location":"#Fixed-Point-API","page":"Home","text":"OptimKit also provides a general fixed-point driver:\n\nx, g, numfp, history = fixedpoint(fp, x0, algorithm; kwargs...)\n\nwhere fp(x) returns the next iterate candidate, corresponding to the iteration process xₙ₊₁ = fp(xₙ). Here, x can be of any possible type, but operations on x are controlled via the keyword arguments, discussed below. The third positional argument algorithm is discussed in the \"Algorithms\" subsection below.\n\nThe return value consists of\n\nx: final iterate\ng: residual at the final iterate, defined as g = invretract(x, fp(x)) (see below)\nnumfp: number of calls to fp\nhistory: residual norms over iterations, returned as a vector length numiter.","title":"Fixed-Point API"},{"category":"section","location":"#Keyword-arguments-related-to-the-geometry-2","page":"Home","text":"The first list of keyword arguments is given by retract, invretract, inner, scale!, add!, transport!, isometrictransport. Their values should be functions that specify the manifold structure and of the configuration space and its tangent space. Their signature, behavior and default values are exactly the same as discussed above in the case of optimize, except for invretract which was not needed for optimize, and acts as\n\nη = invretract(x₀, x) returns the tangent vector η such that   retract(x₀, η, 1) == x, i.e. it quantifies the separation between two points x and   x₀ on the manifold in terms of a tangent vector at base point x₀.\nThe default value of invretract assumes that points live in a Euclidean vector space   and corresponds to invretract(x₀, x) = VectorInterface.add!!(x, x₀, -1).","title":"Keyword arguments related to the geometry"},{"category":"section","location":"#Additional-keyword-arguments-2","page":"Home","text":"The fixedpoint function accepts more keyword arguments that can be used to control the fixed-point iteration process, namely finalize!, hasconverged and shouldstop. They have the same signature, behavior and default values as discussed for the case of optimize above. As there is no cost function in the context of fixedpoint, a sentinel value f = false is passed instead, whereas the residual g = invretract(x, fp(x)) and its norm are passed instead of the gradient of the cost function.","title":"Additional keyword arguments"},{"category":"section","location":"#Fixed-point-algorithms","page":"Home","text":"The third positional argument to optimize can take on of the following values\n\nSimpleIteration(; maxiter = ..., gradtol = ..., verbosity = ...)\nAndersonMixing(m::Int = 8; damping = 1, maxiter = ..., gradtol = ..., verbosity = ...)\n\nAndersonMixing is experimental and may change.","title":"Fixed-point algorithms"}]
}
